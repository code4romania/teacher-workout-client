import React from 'react'
import { Layout } from 'admin/layout'
import { Title } from 'admin/profile/title';
import { Content } from 'admin/profile/content';

export const Profile = () => (
  <Layout>
    <Title />
    <Content />
  </Layout>
);
